---
# playbook.yml -- Ready-to-use playbook for {{ role_name }}
# Generated by ansible-know
#
# Usage:
#   ansible-playbook assets/playbook.yml -i /path/to/inventory.yml --check --diff
#   ansible-playbook assets/playbook.yml -i /path/to/inventory.yml --diff
#
- name: "{{ short_description }}"
  hosts: all
  roles:
    - role: {{ role_name }}
{% if entry_points.get("main", {}).get("options", []) %}
{% for o in entry_points["main"]["options"] %}
{% if o.default is not none %}
      # {{ o.name }}: {{ o.default }}
{% endif %}
{% endfor %}
{% endif %}
